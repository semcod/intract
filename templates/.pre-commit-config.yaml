repos:
  - repo: local
    hooks:
      - id: intract-staged
        name: intract intent contracts (staged)
        entry: intract check --staged --hunks
        language: system
        pass_filenames: false
        stages: [pre-commit]
